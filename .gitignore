# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# 仮想環境
.venv/
venv/

# ツールキャッシュ
.ruff_cache/
.pytest_cache/

# マシン固有の設定。**コミットしてはいけない**。
# --write-config は VRAM やコア数を書き込むので、これを別のマシンに持って
# いくと「そのマシンには無い GPU」を前提に計画が立つ (実際にやらかした)。
# 雛形は gguf-fit.example.toml のほうを追跡する。
gguf-fit.toml

# このツールの出力 (実行した場所に溜まる)
gguf.json
gguf_report.txt
*.gguf

# OS
.DS_Store

# 作業用の出力・退避先 (リポジトリの成果物ではない)
_OUTPUTS/
_to_delete/
