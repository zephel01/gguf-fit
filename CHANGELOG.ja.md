# 変更履歴

主な変更点。English: [CHANGELOG.md](CHANGELOG.md).

まだリリースを切っていないので、`pyproject.toml` の `version` は `0.1.0` の
ままです。日付はコミット日です。

## Unreleased

### 追加

- **`gguf-fetch`** — 4本目のコマンド。Hugging Face から GGUF を落としますが、
  **落とす前に**どれが載るかを決めます。ファイル一覧は数 KB、GGUF ヘッダは
  HTTP `Range` で1本ぶん（約 12 MiB）取り、`gguf-plan` と同じ式でファイルを
  選びます。Ornith-1.5-35B の5量子化 — 候補 172 GB — の判定に要した転送は
  12.0 MiB。モードは判定のみ・`--fit`・`--pick`・`--all` の4つ。
- **実測 bpw** の列。ファイルサイズ ÷ パラメータ数で出します。パラメータ数は
  量子化で変わらないので、ヘッダ1本で全行ぶんが出て転送は増えません。検算に
  なるのが `BF16` で、定義上ちょうど 16.00 になるはずのところ実際にそうなり
  ます。おかげでファイル名の問題が数字で見えます: `UD-Q6_K_XL` は実測
  7.41 bpw、`UD-Q8_K_XL` は 9.21 で `Q8_0` の 8.51 より重い。
- **量子化が多いリポジトリ向けの絞り込み**。`--min-bpw` は中身で切り、
  `--spread` は上から N 本ではなく bpw の幅を取って N 本選び、`--only` /
  `--exclude` は名前のグロブで切ります。24量子化のリポジトリで
  `--fit --top 3` が同じビット帯の3本を返していたのが動機です。
- **`--extras {none,mtp,all}`** — サブディレクトリの GGUF（`MTP/`・
  `imatrix/` など、量子化名**ではない**フォルダ）は候補にしませんが、draft/MTP
  は本体と組にして使うものなので一緒に落とせるようにしました。既定は `none`。
- `--dry-run`・`-y`・`--json`・`--revision`・`--mmproj`・`--probe`・`--hf-bin`。
- **`docs/`** — コマンド・設定・アーキテクチャのリファレンス。
- `gguf-calibrate --write-config` が `kv_measured_on` と
  `kv_derived_f16_bytes` を書き残すようになりました。較正値をどのモデルで
  測ったか照合できます。

### 修正

- **量子化名のフォルダに入っているモデルが落とせなかった。**
  `unsloth/Qwen3.8-Flash-Next-GGUF` はシャード3本を丸ごと `UD-IQ1_S/` に入れ、
  ルートには README しか置いていません。下の「ルート直下だけを候補にする」
  の巻き添えで候補が全部消え、**「このリポジトリに GGUF がありません」**で
  止まっていました。ファイルは目の前にあるのに落とす手立てが無い状態です。
  **フォルダ名そのものが量子化ラベルとして読めるもの**（`UD-IQ1_S`・
  `Q4_K_M`・`BF16`）は候補にし、ラベルはフォルダ名を使うようにしました
  （`--pick UD-IQ1_S` で当たります）。`MTP`・`imatrix`・`original` は量子化名
  として読めないので従来どおり除外、量子化フォルダの中の `mtp`/`draft` も
  同じです。bpw と `n_tensors >= block_count` の網はその上でそのまま効きます。
- **リポジトリの GGUF が全部「本体」ではない。** `unsloth/Qwen3.8-27B-GGUF`
  には `MTP/mtp-…-Q4_0.gguf` (1.28 GiB) が入っています。これを候補に入れて
  いたせいで、ラベル `Q4_0` が本物の 14.95 GiB と衝突し、しかも一番小さいので
  代表に選ばれ、その 4.0 KB/token（KV層 1/65）が全行に当たっていました。
  本物は 68.0 KB/token（17/65）で **17倍**ずれます。しかも表はそれらしく
  出ます。代表は大きいほうから試して `n_tensors >= block_count` を満たすもの
  だけにし、衝突したラベルはファイル名まで伸ばすようにしました。
  （当初はここで「ルート直下だけを候補にする」もやっていましたが、その部分は
  上の項目のとおり置き換えました。）
- **ルートに置いてある、重みではないファイル。** 同じリポジトリの
  `imatrix_unsloth.gguf` はルートにあり — 13 MiB、27B のパラメータ数に対して
  0.004 bpw — 置き場所では弾けません。`--spread` がこれを幅の下端として
  選んでいました。0.5〜33 bpw の外にある候補は外します。名前ではなく数字で
  判断しています。
- **あるモデルで測った較正値が別のモデルに当たっていた。** Qwen3.8-27B の
  69.1 KB/token が、KV が 22.0 しかない Ornith-1.5-35B に使われ、最大 ctx を
  3倍近く低く出していました。両コマンドが計算値どうしを比べ、1.15倍を超えたら
  警告します。
- **`gguf-fetch` が `llama_servers` を見ていなかった。** 複数形のキーしか
  読んでおらず、単数形・環境変数・`--llama-server` を無視していました。CUDA と
  ROCm と Vulkan のビルドが同居しているマシンでは、`gguf-plan` と
  `gguf-fetch` が別の GPU を前提に別の予算を出しえます。どちらも
  `_config.resolve_llama_servers()` を通します。
- **`gguf-fetch --show-config` が `device` を隠していた。** 予算はデバイスの
  容量から取っているので、そこを伏せると数字の出どころに答えていません。
  `device`・`llama_servers`・検出結果を `gguf-plan` と同じように出します。
- **`gguf-fetch` が `gguf-plan` の注意書きを1つも出していなかった** —
  バックエンド混在・空きメモリ不足・ドライバとランタイムの食い違い・VRAM の
  不一致。`plan.budget_warnings()` として共有しました。
- **「MB」と書いて MiB で計算していた。** GB と GiB の警告ボックスを README に
  置いているツールでこれをやっていました。転送量は `MiB` と出ます。
- **`--pick` が候補外のファイルに当たらなかった。** 出力では「要るなら
  `--pick` で名指ししてください」と案内していたのに。
- **`--dry-run` がディスク不足のときに黙っていた。** コマンドを出さずに終わって
  いました。コマンドは出し、終了コードだけ非ゼロのままにします。
- **日本語の表を文字数で詰めていた。** 日本語のセルがある列は必ず崩れます。
  表示幅で揃えるようにしました（`gguf-plan` の表も）。

### 止めるようにしたこと

- 書き込む前にディスクの空きを確かめます。
- HTTP の `Range` を無視するサーバでは、ファイル全体を受け取らずに止めます。
- git の作業ツリーの中に落とそうとすると警告します。`.gitignore` に `*.gguf`
  が入っていると `git status` は綺麗なままで、72 GiB が置きっぱなしになります。

## 2026-08-16

- `gguf-plan --target ollama` / `lmstudio`。GPU に載せる層数の目安は、
  目安と分かる形で出します。
- `gguf-calibrate` — 定数を焼き込まず、そのマシンで測ります。推論そのものが
  確保する分を含めるためにウォームアップを入れ、`--write-config` は
  ファイルの他の部分を触らずに結果だけ書きます。
- 設定に実測の KV 単価があれば `gguf-plan` がそちらを使います。
- ハードウェア検出を `llama-server --list-devices` 主体にし、AMD 対応、
  複数バイナリ、ドライバ側での「空き」の裏取り、そして予算を
  **実際に起動するデバイス**から取るようにしました。
- `--refresh` と、マシン固有の設定ファイルが別のマシンに渡る事故の防止。
- 最初のコミット: `gguf-probe`・`gguf-plan`。
